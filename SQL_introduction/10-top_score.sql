-- list all records of the table second_table of the database.
-- both the score and the name are displayed
-- records should be ordered by score (top first)
SELECT `score`, `name`
FROM `second_table`
ORDER BY `score` DESC;
